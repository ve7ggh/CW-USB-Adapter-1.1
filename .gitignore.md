# --- OS Specific ---
.DS_Store
Thumbs.db
*.swp

# --- Arduino/PlatformIO ---
bin/
obj/
*.elf
*.hex
libdeps/
.pio/
.vscode/
build/
*.gen.h

# --- EasyEDA / CAD ---
# We want to keep the .epro and Gerbers, but ignore local backups if they exist
*.log
*.bak

# --- Bootloader Build Artifacts ---
# We keep the final binaries in your /binaries folder, 
# but we ignore the intermediate objects created during a local 'make'
bootloader/builds/
*.o
*.d
*.map

# --- Python/Misc ---
__pycache__/
*.pyc